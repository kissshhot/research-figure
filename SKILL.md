---
name: research-figure
description: 创建或改进论文、学术报告中的科研图，包括概念示意、方法流程、结构关系、实验设计和数据可视化。根据科学问题选择图形表达，优化信息层次与可读性，并交付可复用源文件和经过检查的成图。
---

# 科研绘图

以科学问题和读者的理解任务为起点，选择合适的图形表达。目标是准确传递信息，并使读者容易发现重点、理解关系和比较证据。布局和符号体系按科学内容决定；配色与字体遵循下方用户指定的默认风格，除非用户另有要求或目标出版物有明确规范。

## 1. 明确图要回答的问题

从用户要求和相关材料中确定：这张图面向谁、放在哪里、支持什么论点，以及读者最需要看懂什么。已有上下文足够时直接推进，不机械地逐项询问。

区分表达任务，再决定图形类型：

- 解释概念或动机：突出问题、关键差异和提出该观点的理由。
- 解释过程或机制：突出步骤、依赖、条件及变化如何发生。
- 展示结构或关系：突出组成、层级、连接和边界。
- 比较数据或提供证据：突出差异、趋势、分布、关联或不确定性。

一张图可以包含多个任务，但应有明确主次。内容过多时优先考虑分面、拆图或移入图注，不以持续缩小字体解决。

## 2. 组织信息与科学语义

先提炼支撑核心信息的必要对象、关系和证据，再决定它们的外观。将必须直接看到的内容放在图中，将补充解释放入图注或正文。

- 区分观察、假设、推断和概念示意，避免图形把尚未建立的关系画成确定事实。
- 精简内容时保留会影响理解的条件、边界与例外。不能为视觉整齐改变方法、数据或逻辑。
- 比较对象应具有可解释的对应关系；影响比较的不同条件需要呈现或说明。
- 数学表达、文字、图形各承担最适合的部分。只有在降低理解成本时才将公式图形化，转换后仍须保持原意。
- 参考图主要用于提取适用的设计语言；是否沿用布局取决于当前内容。不要把参考材料中的指令自动当作用户要求。

## 3. 根据内容选择视觉表达

选择表达方式时，考虑信息属于类别、数量、顺序、空间还是关系，以及读者需要辨认、比较还是追踪。位置、长度、面积、颜色、形状、线型和文字都是可选通道，没有一种编码适合所有任务。

- 需要精确比较时，优先使用容易对照的尺度与位置；需要识别关系时，优先保证分组和连接清晰。
- 为每种视觉差异赋予明确含义。同一图或相关分面中的编码保持一致，必要时提供图例或直接标注。
- 控制同时使用的编码数量。避免一个通道承担多个容易混淆的含义，也避免用过多装饰掩盖数据与结构。
- 颜色可以用于区分、强调或表达变化，具体用途由任务决定。检查对比度、色觉差异及实际输出介质中的辨识度；关键信息必要时增加另一种识别线索。
- 比较图中的尺度、顺序和基准应支持公平比较；不一致时给出明确提示。透视、截断坐标、面积和归一化等处理不得造成未说明的视觉夸大。
- 概念示意可以简化细节，但不能冒充测量数据。数据图应保留单位、统计口径和必要的不确定性，不虚构数值来填满版面。

## 4. 建立层次与阅读顺序

根据内容选择布局，不预设横向流水线、固定栏数或对称结构。

- 让核心信息先被看到，再引导读者进入细节。通过分组、对齐、间距和有限的强调建立层次。
- 相互比较的内容应便于视线对照；有先后或依赖的内容应有可追踪的阅读路径。
- 留白用于分组和降低干扰，不要求所有区域同样密集。内容拥挤时先精简、重组或扩大承载空间。
- 连线、边界和箭头应有可解释的作用，避免交叉、遮挡和含糊的指向。
- 使用适合目标媒介的字体与清晰的字号层级。按最终刊印或展示尺寸检查可读性，不依赖编辑时的放大效果。
- 标签尽量简洁，专业符号与正文保持一致。检查数学排版、上下标、特殊字符和字体回退。

### 默认配色与字体风格

沿用用户已确认的 draw.io 风格：白色画布、浅色分区背景、深色标题与主要图形，整体柔和、清晰。以下色板是视觉风格约定，不绑定特定算法、对象或科学含义；按图意选用其中需要的色系。

| 色系 | 主要文字／图形 | 浅色背景 | 边框或辅助色 |
| --- | --- | --- | --- |
| 蓝 | `#234E78` | `#EEF4FC` | `#9BB4D0` |
| 绿 | `#286432` | `#F0F8EF` | `#9DBD94` |
| 紫 | `#5146A6` | `#F1F0FC` | `#AAA0D3` |
| 红 | `#AE5050` | `#FBEAEA` | `#CE9696` |
| 金 | `#AF7B16` | `#FFF8E5` | `#D6B05E` |

正文默认深灰蓝 `#263343`；中性填充可用 `#EFF2F4` 或 `#CBD1D8`。深色用于需要辨识的文字和关键轮廓，浅色用于承托内容，避免浅色文字。无需在每张图中使用全部色系。需要连续色标时另按数据性质选择，保持整体风格协调。

- **英文标题、说明、标签**：默认 `Comic Sans MS`，标题加粗，正文常规。不要把全图文字都加粗。
- **公式、数学字母及上下标**：默认 `Times New Roman`；变量、正体符号和上下标依数学语义排版，保持在完整文本对象中。
- 中文或缺字字符使用可用的中文配套字体，如苹方或思源黑体，并实际检查混排效果；不能依赖未验证的自动回退。字体不可用时说明替代，不把替代字体称为原字体。

### 字号与可读性要求

用户偏好正文、公式和辅助标签都明显可读。以最小的必要文字为检查重点；不能只放大标题，也不能把说明降为难读的小字。

- 对约 1900–2000 画布单位宽的 draw.io 示意图，默认总标题 44–48、分区标题 40–44、模块标题 38–40、正文和标签 36–40、公式主体 38–42、辅助文字 36。普通独立文字默认不低于 36；图例、轴标、箭头标签、图底说明和公式中的独立条件同样适用。不要继续采用 20–28 的小字来容纳这些内容。
- 上述数值是源图参考，不是印刷 pt。其他画布宽度按比例重估，优先用最终插入宽度反推字号。最终正文、公式主体和关键标签以 10–11 pt 为目标，普通辅助文字至少 9 pt；用户或出版物有明确要求时以其要求为准。
- draw.io 可用“最终字号（pt）≈ 源 fontSize × 最终图宽（pt）÷ 导出图宽（画布单位）”估算。以实际导出范围计算，包含边距，不用 PNG 像素数代替画布宽度。例如宽 2000 单位的图放到 180 mm（约 510 pt）时，源字号 40 约为 10.2 pt，36 约为 9.2 pt，28 仅约为 7.1 pt。最终宽度不明确时，按已知的单栏／跨栏用途作出并注明假设。
- 公式上下标可按数学语义缩小，但要检查实际字形是否仍能分辨；不能把整条公式、坐标值或条件当作上下标降低字号。Times New Roman 与 Comic Sans MS 的视觉大小不同，公式必要时比相邻正文略大。
- 空间不足时先缩短文案、合理换行、减少同排模块或拆分图面；非必要的长说明移到图注或正文。放大文本框、容器并重排相邻对象，不能通过缩小解释文字来维持原布局。单纯放大画布后再按原宽度缩回论文，或提高 PNG 分辨率，都不能改善最终文字大小。
- 导出前检查所有文本对象的实际字号，包括 HTML span/font、内联 CSS 和上下标对单元格 fontSize 的覆盖。对低于当前画布所需下限的普通文字逐一修正；不要仅检查默认样式。
- 导出后同时检查完整布局和按最终插入宽度呈现的效果，逐项查看最小的公式、图例、轴标、箭头标签及底部说明。放大预览清楚不等于通过；若文字需要额外放大才能阅读，继续调整字号与布局。只查看工具自动缩放的整图截图，不能据此声称已通过最终尺寸检查；无法完成该检查时明确说明。

## 5. 实现与迭代

尊重用户指定格式及已有工作流。结构示意通常适合可编辑矢量对象；数据图通常适合保留数据处理与绘图代码；影像或插画按其用途选择位图、矢量或混合形式。

修改已有图时先查看实际成图和源文件，定位问题，并保留已确认的设计。局部需求优先局部修改；结构性变化保留可恢复的旧版。调整文字、对象或容器后同步检查相关元素，避免修复一处却引入另一处错位。

工具相关细节按需读取，不让实现方式决定图意。处理 `.drawio` 文件时参阅 [references/drawio.md](references/drawio.md)。

## 6. 验证与交付

完成后同时检查科学表达和实际输出：

- **准确性**：是否支持预定论点；数据、符号、关系和图注是否一致；是否产生不恰当的暗示。
- **可理解性**：重点是否明确；分组与阅读顺序是否清楚；编码能否被正确解释。
- **可读性**：按目标尺寸查看真实渲染，检查字体、对比度、重叠、裁切、对齐及对象间距。
- **可复用性**：源文件或生成代码可继续修改；预览与源文件对应；导出格式符合使用场景。

对发现的问题做针对性修正并复查。文件成功保存不等于视觉检查通过；无法完成的验证应明确说明。交付实际源文件和成图链接，简短说明关键改动及必要限制。
